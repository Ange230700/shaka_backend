# .env.test — disposable E2E database (used locally & in CI)
NODE_ENV=test
FRONT_API_BASE_URL=http://localhost:5173

# MySQL (CI service/local docker)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DB=shaka_test

# Prisma/ShakaDB connection (dotenv doesn't expand vars by default, so keep it literal)
DATABASE_URL="mysql://root:root@127.0.0.1:3306/shaka_test"
